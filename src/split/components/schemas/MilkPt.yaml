required:
  - dim
  - milk
type: object
properties:
  dim:
    type: number
    format: integer
    description: Days since calving date
  milk:
    type: number
    format: float
    description: >-
      quantity of milk produced in one day, or a fraction of a day if
      day_fraction is supplied. Milk is measured in units specified by
      milk_unit, defaulting to kg.
  milk_unit:
    $ref: ./MilkUnit.yaml
  day_fraction:
    type: number
    format: float
    default: 1
    description: >-
      fraction of one day's production that **_milk_** represents, defaulting to
      1.0
description: Measured milk production data point.
